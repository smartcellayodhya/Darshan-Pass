# WORKSPACE AUTOMATIC DEPLOYMENT RULE

Whenever code or design changes are made to `d:\antigravity\Darshan pass`:
1. Automatically run `git add .`
2. Automatically commit with a meaningful commit message.
3. Automatically run `git push origin main`.
4. This ensures Vercel automatically deploys every change to the live website instantly without requiring any manual user steps.
